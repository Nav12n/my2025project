import { Highlightdiretives } from './highlightdiretives';

describe('Highlightdiretives', () => {
  it('should create an instance', () => {
    const directive = new Highlightdiretives();
    expect(directive).toBeTruthy();
  });
});
